# 当前进度

更新日期：2026-09-18。

## 终版主线

终版目标已经固定为：物理 CPU0 运行 CP NuttX/OpenVela，物理 CPU1、CPU2
运行 AP SMP NuttX/OpenVela。历史的 CPU0 Armino + AP OpenVela 固件只作为
外设硬件对照和回退资料，不再继续开发其产品应用或声网方案。

| 工作项 | 当前状态 | 证据/下一步 |
| --- | --- | --- |
| 参考 BSP 固定与许可证核对 | 已完成 | 135 队固定为 `7079493e...`; SDK 固定为 `cb080de...`; 见 `SOURCE_PROVENANCE.md` |
| CPU0-only OpenVela | 本队 R1 实机通过 | NSH/`uname` 日志在 `evidence/cpu0-only/` |
| CPU0 + AP + CPU2 全 OpenVela 实验 | 本队 R1 有单次成功证据 | AP READY、RPTUN CONNECTED、CPU2 SMP PASSED、Wi-Fi 当次成功；见 `evidence/full-openvela-experimental/` |
| 终版 BSP 源码整合 | 已完成源码层整合 | `chips/`、`boards/`、`nuttx/`、`tools/` 已纳入，未混入参考仓产品应用 |
| 本队 CP/AP 配置 | 已完成生成配置核验 | CP/AP `.config` 已检查；关键 BSP 选项为 `y` |
| AP/RPTUN/SMP 诊断 | 已编入新的 CP 镜像 | `apctl` 已出现在 CP ELF；待烧录后执行 |
| Wi-Fi VNET 诊断 | 已编入新的 CP 镜像 | `bkwifi` 已出现在 CP ELF；待烧录后执行 |
| 双屏、Camera/JPEG、Audio、电源 | 历史混合固件已验证硬件链 | 不能替代终版；终版按双屏→Camera→Audio→电源顺序回归 |
| 声网实时语音 | 已停止 | 无额度且不作为终版 BSP 依赖，不继续接入 |
| MiMo/长期记忆/产品应用 | 暂不纳入终版主线 | 保留历史源码，等 BSP 稳定后再决定是否接回 |
| AI Coding 日志 | 已归集并通过格式校验 | `logs/xxluestc/`：3 个文件、76 个事件；仍需按官方流程补导最新会话 |
| 自建 Skill | 已存在并通过 12 项主机测试 | `.agents/skills/bk7258-openvela-porting/` |
| 官方技术报告 | 已生成 12 页候选稿 | `submission/技术报告-终版候选.docx/.pdf`; 待填成员、最终哈希和终版实测数据 |

## 提交前门禁

以下事项必须按顺序完成，不能用参考仓证据替代：

1. Linux 工作区干净构建 CP/AP；
2. 核对生成的 CP/AP `.config`、ELF/map 地址、bin 大小和构建清单；
3. 生成终版镜像和 SHA256，明确只写入允许的 Flash 区域；
4. 由用户烧录；
5. 保存终版启动、`apctl status`、`bkwifi`、双屏和至少一项多媒体日志；
6. 回填报告中的提交 commit、镜像 SHA256、测试次数和真实结果；
7. 全仓敏感信息扫描、AI Coding 日志校验、报告版式检查；
8. 合并到本仓 `dev-ai-contest-2026` 并推送。

## 当前阻塞

新的 CP/AP 构建已经在 Linux 虚拟机完成，当前只剩用户烧录和实机回归。Windows 本地
已经完成 manifest XML、Python 入口、自建 Skill 测试、报告生成和证据脱敏检查。

## 最终需补齐的真实数据

- 终版 Git commit、CP/AP ELF/bin/镜像 SHA256；
- 工具链版本和生成 `.config` 关键项；
- 至少 10 次冷启动成功/失败统计；
- `free`、map 或高水位得到的 SRAM/PSRAM 使用量；
- Wi-Fi 连接/重连结果；
- 双屏、Camera 或 Audio 至少一条终版回归；
- 真机照片或录屏。

没有原始日志或测试方法的数据不写入技术报告。

## 2026-09-18 最终四段镜像实机节点

最终四段镜像已烧录到 R1，镜像文件与 `build-manifest.json` 的大小和 SHA256 一致。
冷重启后未手动执行 `rptun start`，串口确认：

- CP NuttX 启动，`uname -a` 为 `NuttX ... 76354c63785 arm aidk_ai_toy`；
- AP 自动启动，CP/AP 日志均出现初始化信息；
- SDIO/MMCSD 初始化完成；
- 两块 GC9D01 屏完成初始化，注册 `fb=0`、`fb=1`；
- GC2145 Camera 注册 `/dev/video0 640x480@30 MJPEG`；
- 跨核 `rptun ping /dev/rpmsg/ap 1 16 1 100` 返回，单次约 13.8 ms；
- CP 侧可见 `/dev/rptun/ap`、`/dev/rpmsg/ap`。

本节点证明了 CP 启动、AP 自动启动、RPTUN/RPMsg、SDIO、双屏和 Camera 的初始化链路。
当前 CP 镜像没有编入 `apctl`、`bkwifi` 诊断应用，`help` 中找不到这两个命令；原因是
VM 构建工作区缺少 `apps/system/contest2026_441_bsp_diag` 链接。它不影响本节点已经
验证的 BSP 启动和跨核链路，但下一次构建前需要补齐该工作区接入，再验证诊断命令。

已在 VM 中将该应用接入为真实目录（不能只用符号链接，否则 CMake 首次扫描不会纳入）：
`/home/alientek/ov441_ws/apps/system/contest2026_441_bsp_diag`，来源为
`/home/alientek/ov441_ws/team-repo/app/bsp_diag`。重新配置后，CP ELF 已能检索到
`apctl`、`bkwifi` 及其帮助字符串。

本次构建结果（尚未烧录）：CP `916810` 字节，SHA256
`ab85fe43815c111e0ae9ef7d1b07d1d559bf627a08552903d9ed2a7808d5174a`；Pair
`2854912` 字节，SHA256
`b829ff59be5a3d0ff52c2c502ca7a843c10d95d955c8b7e86867e882a8a75a78`。Boot/AP
与上一版一致。对应清单已同步到 `G:\CIE\R1\firmware\build-manifest.json`，烧录脚本会先核对清单。

下一验证节点：用户烧录新的四段镜像后，先执行 `help`、`apctl status`、
`rptun ping /dev/rpmsg/ap 1 16 1 100`，再执行 `bkwifi status`。不手动执行
`rptun start`，因为 AP 已由 CP 自动启动，手动启动会与自动启动竞态。

当前仍未宣称：Wi-Fi 实际联网、Camera 采帧、音频录放、应用层功能和最终提交已通过。

## 2026-09-18 诊断镜像终版真机节点

新的 CP/Pair 镜像已由用户烧录，串口验证结果如下：

- `help` 已出现 `apctl`、`bkwifi`，说明诊断应用确实进入 CP 镜像；
- `apctl status`：AP=`READY`，RPTUN=`CONNECTED`，AP supervisor=`HEALTHY`；
- CPU2=`SCHEDULER_ONLINE`，AP SMP=`PASSED`，故障和恢复次数均为 0；
- `sleep 3` 正常返回，前后心跳从 3674 增长到 3702；
- `rptun ping /dev/rpmsg/ap 1 16 1 100` 成功，单次往返约 14.15 ms；
- `bkwifi status` 命令正常返回，当前未连接 Wi-Fi（link=0、IP 为 0.0.0.0），这不等于 Wi-Fi 驱动失败。

本节点确认了新的终版 CP 诊断应用、AP 自动启动、RPTUN、CPU2 SMP、系统节拍和跨核
链路。尚未验证 Wi-Fi 实际联网、双屏、Camera 采帧和音频链路；这些仍需单独测试，
不能由本节点替代。
