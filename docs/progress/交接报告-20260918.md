# R1 项目交接报告（2026-09-18）

## 1. 接手时先看这里

- 最终候选工作树：`G:\CIE\contest2026_441_final`
- 当前分支：`codex/final-submit-20260916`
- 当前提交：`4b5be60 记录诊断应用编入终版构建`
- 上一提交：`ea674c0 补齐BSP诊断应用工作区链接`
- 工作树当前干净，本地比用户远程分支领先 2 个提交，尚未推送。
- 用户仓库远程：`fork`，目标分支为 `dev-ai-contest-2026`。
- 不要改动或清理原来的脏工作树：`G:\CIE\contest2026_441_buzhidaojiaoshenmemingzi`。
- 截止日期：2026-09-20。当前第一优先级是烧录新的 CP/Pair 镜像并完成真机验证和终版提交。

## 2. 当前技术路线

最终候选采用全 OpenVela/NuttX 多核 BSP：

```text
物理 CPU0：CP NuttX
  └─ 启动并管理 AP、提供诊断命令

物理 CPU1 + CPU2：AP 双核 NuttX SMP
  └─ 运行板级外设、网络及后续应用

CP ↔ AP：Mailbox / Doorbell / RPTUN / 共享内存
```

主要参考仓库已固定到：

- 仓库：`open-vela/contest2026_135_yongwangzhiqian`
- 参考提交：`7079493e73159e00c1b03e60bee6ee69845751cd`
- 审查副本：`G:\CIE\R1\research\reference-audit-20260916\contest2026_135_yongwangzhiqian`
- OpenVela SDK 固定提交：`cb080de1655d579c7593ecf504c440997c4c137b`
- 来源、许可证和纳入范围见 `SOURCE_PROVENANCE.md` 与 `docs/BK7258参考BSP审查.md`。

只整合了 BSP、启动、内存、中断、系统节拍、跨核通信、板级代码和构建工具；没有整仓覆盖，也没有引入对方的产品应用、云端服务、模型或凭据。

## 3. 已完成内容

- BSP 代码范围：
  - `chips/bk7258`
  - `boards/bk7258/common`
  - `boards/bk7258/aidk_ai_toy`
  - `nuttx`
  - `tools/bk7258`
- 团队配置：
  - CP：`boards/bk7258/aidk_ai_toy/configs/team441_cp`
  - AP：`boards/bk7258/aidk_ai_toy/configs/team441_ap`
  - `openvela.conf` 已选择以上配置。
- 自建诊断应用：`app/bsp_diag`
  - `apctl`：检查 AP、RPTUN、SMP 和心跳。
  - `bkwifi`：检查 Wi-Fi 服务和网络状态。
- 自建 Skill：`.agents/skills/bk7258-openvela-porting/`
- 技术报告候选：
  - `submission/2026首届openvela大赛作品提交模板.docx`
  - `submission/技术报告正文草案.md`
  - `submission/技术报告-终版候选.docx`
  - `submission/技术报告-终版候选.pdf`
- AI Coding 日志：`logs/xxluestc/`，官方校验器已通过 3 个文件、76 个事件。

声网方案已停止，不再继续开发，也不要要求用户充值。历史语音成果保留，但不能把混合固件或旧语音镜像的验证结果当作最终全 OpenVela BSP 的验证证据。

## 4. 已通过的本地检查

```text
python -B tests/host/bk7258/test_layers_crlf.py                 通过
python tools/bk7258/bk7258.py verify layers                    通过
分层检查结果：sources=330，kconfig=251，legacy-exceptions=2
自建 Skill 测试：12 项通过
官方 AI Coding 日志校验：通过
manifest XML 解析：通过
已知密钥、Wi-Fi 名称和密码扫描：未发现泄漏
技术报告 PDF：13 页，3.1～3.7 齐全，无模板占位符
```

现有证据目录：

- `evidence/cpu0-only`
- `evidence/full-openvela-experimental`
- `evidence/mixed-reference`
- `evidence/camera`

证据等级必须如实保留；只有最终候选镜像的重新构建和真机日志，才能写入 `evidence/final/`。

## 5. 当前主要阻塞

Linux 构建已经实际完成。此前的 SSH 问题已恢复，不再是当前阻塞。新的四段镜像已经
同步到 `G:\CIE\R1\firmware`，但尚未由用户烧录，因此 `apctl`、`bkwifi` 还没有新的
终版真机证据。

## 6. 下一步按此顺序执行

1. 用户烧录新的四段镜像；烧录始终由用户操作。
2. 最小真机验证：

   ```bash
   uname -a
   help
   apctl status
   sleep 3
   apctl status
   rptun ping /dev/rpmsg/ap 1 16 1 100
   bkwifi status
   ```

   通过标准：AP 为 READY、RPTUN 为 CONNECTED、SMP 为 PASSED、心跳递增。
3. 再验证双屏和一个多媒体链路；每项保存原始串口日志、日期、提交号和镜像哈希到 `evidence/final/`。
4. 用真实结果更新：
   - `docs/系统架构.md`
   - `docs/progress/当前进度.md`
   - `docs/progress/R1移植实测记录.md`
   - `docs/progress/构建与镜像记录.md`
   - 技术报告终版
5. 再做一次密钥扫描、日志校验、报告目视检查；确认无误后才推送到用户仓库的 `dev-ai-contest-2026`。不要推送到公开上游 `origin`。

## 7. 提交前仍需补齐

- 新构建的 CP/AP `.config` 已确认，CP 诊断应用选项为 `=y`。
- 新的 CP/AP 镜像、ELF/map 地址和 SHA256 已记录；仍缺用户烧录后的真机证据。
- 最终全 OpenVela 真机启动、跨核、双屏及至少一个外设链路证据。
- 用户真实的队伍/成员信息。
- 有日志依据的性能数据；没有测量就不要填写推测值。
- 开发板照片或录屏。
- 当前长对话若客户端支持，应由用户主动导出；本机 staging 中没有可伪造或补写的会话日志。

## 8. 接手约束

- 不提交 API Key、Wi-Fi 密码、Token、管理口令或本机凭据。
- 不宣称未构建、未烧录或未实测的功能已经通过。
- 不再新增声网工作。
- 不覆盖用户原工作树，不执行破坏性 Git 操作。
- 不为每个小改动创建零散文档；继续维护现有总图、构建记录和实测记录。
- 优先复用已经审查过的 BSP 实现，不凭接口名称猜测行为。
